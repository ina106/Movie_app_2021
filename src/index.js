import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// App.js 파일에서 App 함수(컴포넌트)를 임포트하여 사용했다 이런 뜻

ReactDOM.render(<App />,document.getElementById('root'));
//App 컴포넌트는 아이디가 root인 엘리먼트에 그려질(render) 것이다

